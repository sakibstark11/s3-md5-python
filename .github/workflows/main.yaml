on: [push]

jobs:
  install-modules:
    runs-on: ubuntu-22.04
    name: Install modules
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run tests
        working-directory:
        id: run-tests
        uses: ./
        with:
          args: "python3 -m pytest"
