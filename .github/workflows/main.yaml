on: [push]

jobs:
  install-modules:
    runs-on: ubuntu-22.04
    name: Install modules
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Install dev dependencies
        uses: ./ 
        with:
          command-to-run: "pip3 install -r dev-requirements.txt"
